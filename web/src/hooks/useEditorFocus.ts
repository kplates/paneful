// This hook is intentionally empty now.
// Editor sync is handled via WebSocket (server pushes editor:active messages).
// The toggle and setup instructions live in the Sidebar header.
// Keeping this hook so we don't need to remove it from App.tsx.
export function useEditorFocus() {}
